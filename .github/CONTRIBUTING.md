# Contributing to the project

The main part of this project is within the graphviz-java/ directory. 
Before contributing, you need to make sure you have graphviz and maven installed.

```bash
sudo apt install graphviz
sudo apt install maven
```

